// habit_card.dart
// Widget for displaying a habit card

import 'package:flutter/material.dart';

class HabitCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
